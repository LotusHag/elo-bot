<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Custom Game Setup</title>
    <style>
        body {
            background-color: #01133D;
            color: white;
            font-family: Arial, sans-serif;
        }
        .nav-container {
            display: flex;
            gap: 10px;
            margin-bottom: 20px;
        }
        .nav-container a {
            padding: 10px;
            background-color: #333;
            border: 1px solid #555;
            text-decoration: none;
            color: white;
        }
        .nav-container a:hover {
            background-color: #555;
        }
        .form-container {
            background-color: #333;
            padding: 20px;
            border-radius: 5px;
            width: 400px;
            margin: auto;
        }
        .form-container input[type="text"], .form-container select {
            width: calc(100% - 20px);
            padding: 10px;
            margin-bottom: 10px;
            border: none;
            border-radius: 5px;
        }
        .form-container input[type="submit"] {
            background-color: #4CAF50;
            color: white;
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }
    </style>
</head>
<body>
    <div class="nav-container">
        <img src="/images/logo.png" alt="Logo" style="height: 50px; margin-right: 10px;">
        <a href="/players/leaderboard">Player Leaderboard</a>
        <a href="/teams/leaderboard">Team Leaderboard</a>
        <a href="/players/input">Submit Player Results</a>
        <a href="/teams/input">Submit Team Results</a>
        <a href="/custom-game/setup">Create Custom Game</a>
    </div>
    <h1>Custom Game Setup</h1>
    <div class="form-container">
        <form action="/custom-game/setup" method="POST">
            <% for (let i = 0; i < 10; i++) { %>
                <h2>Player <%= i + 1 %></h2>
                <input type="text" name="players[<%= i %>][name]" placeholder="Player Name" required>
                <label for="role">Preferred Role:</label>
                <select name="players[<%= i %>][role]" required>
                    <option value="Top">Top</option>
                    <option value="Jungle">Jungle</option>
                    <option value="Mid">Mid</option>
                    <option value="Bot">Bot</option>
                    <option value="Support">Support</option>
                    <option value="Fill">Fill</option>
                </select>
            <% } %>
            <input type="submit" value="Create Teams">
        </form>
    </div>
</body>
</html>
