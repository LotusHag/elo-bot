<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Submit Game Results - League of Legends</title>
    <style>
        body {
            background-color: #01133D;
            color: white;
            font-family: Arial, sans-serif;
        }
        .nav-container {
            display: flex;
            justify-content: space-between;
            align-items: center;
            gap: 10px;
            margin-bottom: 20px;
        }
        .nav-container a {
            padding: 10px;
            background-color: #333;
            border: 1px solid #555;
            text-decoration: none;
            color: white;
        }
        .nav-container a:hover {
            background-color: #555;
        }
        .form-container {
            background-color: #333;
            padding: 20px;
            border-radius: 5px;
            width: 80%;
            margin: auto;
            margin-top: 50px;
        }
        .form-container input[type="text"], .form-container select {
            padding: 10px;
            border: none;
            border-radius: 5px;
        }
        .form-container input[type="submit"] {
            background-color: #4CAF50;
            color: white;
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }
        .player-row {
            display: flex;
            align-items: center;
            margin-bottom: 10px;
        }
        .player-row input[type="text"] {
            width: 20%;
            margin-right: 5px;
        }
        .player-row select {
            width: 10%;
            margin-right: 5px;
        }
    </style>
</head>
<body>
    <div class="nav-container">
        <div>
            <img src="/images/logo.png" alt="Logo" style="height: 50px; margin-right: 10px;">
            <a href="/">Home</a>
            <a href="/games/<%= game._id %>">Leaderboard</a>
            <a href="/games/<%= game._id %>/custom-game">Create Another Custom Game</a>
        </div>
        <div>
            <% if (user) { %>
                <span>Welcome, <%= user.username %>!</span>
                <a href="/auth/logout">Logout</a>
            <% } else { %>
                <a href="/auth/login">Login</a>
            <% } %>
        </div>
    </div>
    <div class="form-container">
        <h1>Submit Game Results - League of Legends</h1>
        <form action="/games/<%= game._id %>/input" method="POST">
            <h2>Blue Team</h2>
            <% for (let i = 1; i <= 5; i++) { %>
                <div class="player-row">
                    <input type="text" name="blueTeam[]" placeholder="Player <%= i %>" required>
                    <select name="roles[]">
                        <option value="Top">Top</option>
                        <option value="Jungle">Jungle</option>
                        <option value="Mid">Mid</option>
                        <option value="Bot">Bot</option>
                        <option value="Support">Support</option>
                    </select>
                </div>
            <% } %>
            <h2>Red Team</h2>
            <% for (let i = 1; i <= 5; i++) { %>
                <div class="player-row">
                    <input type="text" name="redTeam[]" placeholder="Player <%= i %>" required>
                    <select name="roles[]">
                        <option value="Top">Top</option>
                        <option value="Jungle">Jungle</option>
                        <option value="Mid">Mid</option>
                        <option value="Bot">Bot</option>
                        <option value="Support">Support</option>
                    </select>
                </div>
            <% } %>
            <h2>Winner</h2>
            <select name="winner" required>
                <option value="Blue Team">Blue Team</option>
                <option value="Red Team">Red Team</option>
            </select>
            <input type="submit" value="Submit Results">
        </form>
    </div>
</body>
</html>
