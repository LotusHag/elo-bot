<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Verify Runs</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #2c3e50;
            color: white;
            margin: 0;
            padding: 0;
        }
        .container {
            width: 80%;
            margin: auto;
            overflow: hidden;
        }
        .nav-container {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px;
            background-color: #34495e;
        }
        .nav-container a {
            padding: 10px;
            background-color: #333;
            border: 1px solid #555;
            text-decoration: none;
            color: white;
            border-radius: 5px;
        }
        .nav-container a:hover {
            background-color: #555;
        }
        h1 {
            text-align: center;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin: 20px 0;
        }
        table, th, td {
            border: 1px solid #555;
        }
        th, td {
            padding: 10px;
            text-align: center;
        }
        th {
            background-color: #34495e;
        }
        td {
            background-color: #3b5998;
        }
        .btn {
            padding: 10px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }
        .btn-approve {
            background-color: #4CAF50;
            color: white;
        }
        .btn-delete {
            background-color: #e74c3c;
            color: white;
        }
        .btn-back {
            display: block;
            width: 100px;
            margin: 20px auto;
            background-color: #3498db;
            color: white;
            text-align: center;
            padding: 10px;
            border-radius: 5px;
            text-decoration: none;
        }
    </style>
</head>
<body>
    <div class="nav-container">
        <a href="/">Home</a>
        <a href="/trackmania/submit">Submit New Run</a>
        <a href="/trackmania/verify-runs">Verify Runs</a>
    </div>
    <div class="container">
        <h1>Verify Runs</h1>
        <table>
            <tr>
                <th>Player</th>
                <th>Map</th>
                <th>Time</th>
                <th>Actions</th>
            </tr>
            <% runs.forEach(run => { %>
                <tr>
                    <td><%= run.player ? run.player.name : 'Unknown Player' %></td>
                    <td><%= run.map ? run.map.name : 'Unknown Map' %></td>
                    <td><%= run.time %></td>
                    <td>
                        <form action="/trackmania/verify-runs/approve/<%= run._id %>" method="POST" style="display:inline;">
                            <button type="submit" class="btn btn-approve">Approve</button>
                        </form>
                        <form action="/trackmania/verify-runs/delete/<%= run._id %>" method="POST" style="display:inline;">
                            <button type="submit" class="btn btn-delete">Delete</button>
                        </form>
                    </td>
                </tr>
            <% }) %>
        </table>
        <a href="/" class="btn-back">Back to Home</a>
    </div>
</body>
</html>
